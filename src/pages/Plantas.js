import React from 'react';
import { Text, SafeAreaView } from 'react-native';
import { globalStyles } from '../styles/global';

const Plantas  = () => {

  return (
    <SafeAreaView style={globalStyles.container}>
      <Text>Tela do reino plantas</Text>
    </SafeAreaView>
  );
};


export default Plantas;
